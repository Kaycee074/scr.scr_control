get_distances:
	first argument is the sensor number to get data from
	returns int16[] distances (a 1D array representing a heatmap of the area)
	
get_distances_all:
	returns int16[] distances (a 1D array representing a heatmap of the room)

help:
	prints a list of all commands, what they do, and what arguments they take
